@model IEnumerable<Practica.Models.DetallePedido>
@{
    ViewData["Title"] = "Detalles de Pedido";
}
<style>
    :root {
        --bg: #f4f6f9;
        --fg: #333;
        --primary: #1a73e8;
        --border: #e5e7eb
    }

    body {
        background: var(--bg);
        color: var(--fg)
    }

    .container {
        max-width: 1000px;
        margin: 24px auto;
        padding: 0 16px
    }

    .card {
        background: #fff;
        border: 1px solid var(--border);
        border-radius: 12px;
        box-shadow: 0 8px 24px rgba(0,0,0,.06);
        padding: 24px
    }

    .h1 {
        font-size: 1.6rem;
        margin: 0 0 16px
    }

    .btn {
        display: inline-block;
        padding: .55rem 1rem;
        border-radius: 8px;
        text-decoration: none;
        border: none;
        cursor: pointer
    }

    .btn-primary {
        background: var(--primary);
        color: #fff
    }

    .btn-light {
        background: #f3f4f6
    }

    .btn-danger {
        background: #dc3545;
        color: #fff
    }

    .table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
        border: 1px solid var(--border);
        border-radius: 12px;
        overflow: hidden
    }

        .table th, .table td {
            padding: .75rem;
            border-bottom: 1px solid var(--border)
        }

        .table tr:hover {
            background: #fafafa
        }

    .actions a {
        margin-right: .35rem
    }
</style>

<div class="container">
    <div class="card">
        <div class="h1">Detalles de Pedido</div>
        <p><a asp-action="Create" class="btn btn-primary">+ Agregar detalle</a></p>
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Pedido</th>
                    <th>Producto</th>
                    <th>Cantidad</th>
                    <th>Precio</th>
                    <th>Subtotal</th>
                    <th style="width:230px;">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.DetallePedidoId</td>
                        <td>#@item.PedidoId</td>
                        <td>@item.Producto?.Nombre</td>
                        <td>@item.Cantidad</td>
                        <td>@item.PrecioUnitario.ToString("C")</td>
                        <td>@((item.Cantidad * item.PrecioUnitario).ToString("C"))</td>
                        <td class="actions">
                            <a class="btn btn-light" asp-action="Edit" asp-route-id="@item.DetallePedidoId">Editar</a>
                            <a class="btn btn-light" asp-action="Details" asp-route-id="@item.DetallePedidoId">Detalles</a>
                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.DetallePedidoId">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
